import { QqqDirective } from './qqq.directive';

describe('QqqDirective', () => {
  it('should create an instance', () => {
    const directive = new QqqDirective();
    expect(directive).toBeTruthy();
  });
});
